defaults: &defaults
  allow_dynamic_fields: true
  raise_not_found_error: true
  identity_map_enabled: true


development:
  sessions:
    default:
      hosts:
        - localhost:27017
      database: rails3_extjs4
  options:
    <<: *defaults

test:
  sessions:
    default:
      hosts:
        - localhost:27017
      database: rails3_extjs4
  options:
    <<: *defaults

production:
  sessions:
    default:
      hosts:
        - localhost:27017
      database: rails3_extjs4
  options:
    <<: *defaults